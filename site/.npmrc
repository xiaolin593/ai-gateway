# Enforce Node/npm version requirements from package.json
engine-strict=true

# Allow semver ranges (caret/tilde) for flexibility
save-exact=false

# Skip audits during install (faster builds, can audit separately)
audit=false

# Hide funding messages for cleaner output
fund=false

# Reduce noise in CI/Netlify logs
loglevel=error
